import { Template } from "tinacms";

export const ExampleHeroTemplate: Template = {
    name: 'ExampleHero',
    label: 'Example Hero',
    fields: [
        {
            name: 'title',
            label: 'Title',
            type: 'string',
        },
    ],
}